# Markdown: los documentos de docs/ y la raíz llevan tablas alineadas a mano y
# ancho de 80 columnas. Prettier expande cada tabla al ancho de su columna más
# larga (184 líneas cambiadas solo en CONTINUAR.md, filas de 200+ caracteres) y
# convierte cualquier corrección de una celda en un diff ilegible.
*.md

# Lockfile: lo escribe npm, no una persona.
package-lock.json

# Assets servidos tal cual (incluye el wheel del core para Pyodide).
apps/web/public

# Salida generada por scripts/datos_informe.py (`json.dumps(..., indent=1)`):
# reformatearla la deja sucia hasta la próxima corrida del script, que la
# revierte.
docs/_datos_informe

# Fixtures golden del contrato core ↔ frontend: los escribe
# tests/test_contract_frontend.py con `json.dumps(..., indent=1)` y llevan
# "NO editar a mano" adentro. Mismo caso que el anterior.
apps/web/e2e/fixtures
